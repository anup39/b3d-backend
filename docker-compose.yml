version: "3"

services:
  # node-odm:
  #   image: opendronemap/nodeodm
  #   ports:
  #     - "3000:3000"
  #   restart: unless-stopped
  #   oom_score_adj: 500
  #   volumes:
  #     - ./data:/var/www/data

  postgres:
    image: postgis/postgis:15-3.4-alpine
    env_file: pg_env.txt
    # ports:
      # - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

  web:
    env_file: .env # Change env_sample.txt to env.txt
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    # command:
    #   ["bash", "-c", "chmod +x /code/entrypoint.sh && sh /code/entrypoint.sh"]
    depends_on:
      - postgres

  martin:
    build:
      context: ../martin
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "4000:3000"
    environment:
      - DATABASE_URL=postgres://postgres:postgres@postgres:5432/b3d
      - RUST_LOG=actix_web=info,martin=debug,tokio_postgres=debug
    depends_on:
      - postgres



  # pgadmin:
  #   image: dpage/pgadmin4
  #   env_file: pg_env.txt
  #   ports:
  #     - "5051:80"
  #   restart: always


  geoserver:
    image: kartoza/geoserver:latest
    environment:
      - GEOSERVER_ADMIN_USER=admin
      - GEOSERVER_ADMIN_PASSWORD=geoserver
    volumes:
      -  ./geoserver_data:/opt/geoserver/data_dir
      # -  ./data:/opt/geoserver/data_dir/odm_data

    ports:
      - "8080:8080"
    restart: on-failure

  # redis:
  #   image: redis:alpine
  #   restart: always
  #   command: redis-server --appendonly yes
  #   volumes:
  #     - ./redis_data:/var/lib/redis

  # worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   restart: always
  #   entrypoint: "celery -A project worker -l DEBUG -Q default,b3d --pool=solo"
  #   depends_on:
  #     - web
  #   volumes:
  #     - .:/code
  #     - ./logs/:/logs/
  #   env_file:
  #     - .env

  # beat:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   restart: always
  #   entrypoint: "celery -A project beat --loglevel=debug"
  #   depends_on:
  #     - web
  #     - worker
  #   volumes:
  #     - .:/code
  #     - ./logs/:/logs/
  #   env_file:
  #     - .env

volumes:
  postgres_data:
    name: postgres_data
  # geoserver_data:
  #   name: geoserver_data
  # redis_data:
  #   name: redis_data
  # logs:
  #   name: logs
